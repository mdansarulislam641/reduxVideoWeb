import React from 'react';

const Loading = () => {
    return (
        <div className='flex items-center justify-center h-[80vh]'>
            <h2>Loading....</h2>
        </div>
    );
};

export default Loading;